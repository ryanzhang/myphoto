<view class="image-container">
  <image src="{{imagePath}}" mode="aspectFit" class="image-player" binderror="onImageError"></image>
  <text wx:if="{{imageError}}" class="error-message">图片加载失败</text>
</view>
